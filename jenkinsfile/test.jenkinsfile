pipeline {
  agent {
    node {
      label 'Build-Slaves-B'
      customWorkspace "D:\\Jenkins\\Workspace\\${env.JOB_BASE_NAME}"
    }
  }
  options {
    buildDiscarder(logRotator(daysToKeepStr: '1', numToKeepStr: '1'))
  }
  stages {
    stage("Test") {
      steps {
        script {
          def command = "dir"
          def output = bat (
            script: command
          )
          println output
        }
      }
    }
  }
}